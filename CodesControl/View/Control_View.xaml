<UserControl x:Class="CodesControl.View.Control_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodesControl.View"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="700" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListView x:Name="AviableItems" 
                 Grid.Column="1"
                 ItemsSource="{Binding AviableCollection}"
                 IsSynchronizedWithCurrentItem="True" Background="#FFF7E7E7" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Bisque" 
                            BorderThickness="1" 
                            Margin="10">
                        <StackPanel Margin="10">
                            <TextBlock Text="{Binding UserName}" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="{Binding UserLastName}" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Осталось:" />
                                <TextBlock Text="{Binding UserLogin}"
                                           FontWeight="Bold" 
                                           Margin="10,0"/>
                                <TextBlock Text="шт" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>



        <ListView x:Name="CodesTypes" 
                 Background="#FFEAF8F9"
                 ItemsSource="{Binding CodesType}" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" />
                        <TextBlock Text="{Binding Count}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <ContentControl Grid.Column="2" Content="{Binding AviableCollection}" Grid.RowSpan="2">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <Border BorderBrush="Bisque"
                            BorderThickness="1" 
                            Margin="10">
                        <StackPanel Margin="10">
                            <TextBlock Text="Название:"/>
                            <TextBox Text="{Binding UserName, Mode=OneWay}"
                                     Margin="0,0,0,10"/>

                            <TextBlock Text="Автор:"/>
                            <TextBox Text="{Binding UserLastName, Mode=OneWay}" 
                                     Margin="0,0,0,10"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>
        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="Статус кода" Background="White" Foreground="#FF292929" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="14.667"/>
    </Grid>
</UserControl>
